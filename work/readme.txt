

upgrade = Добавить поиск min/max ребенка с помощью бинарной кучи. Чтобы SiftDown заботал за O(log(k) log_k(N)).

sheap = superheap = kheap with upgrade

hheap = helpheap, used in sheap (stores index and value, index = key)